# 📊 Настройка отправки формы в Google Sheets

## 🎯 Быстрая настройка (5 минут)

### Шаг 1: Создайте Google Таблицу

1. Откройте [Google Sheets](https://sheets.google.com)
2. Создайте новую таблицу
3. В первой строке добавьте заголовки:
   ```
   A1: Дата и время
   B1: Имя
   C1: Телефон
   D1: Сообщение
   ```
4. Сохраните таблицу и скопируйте её ID из URL:
   ```
   https://docs.google.com/spreadsheets/d/ВАШ_ID_ТАБЛИЦЫ/edit
   ```

### Шаг 2: Создайте Google Apps Script

1. В таблице нажмите **Расширения** → **Apps Script**
2. Удалите весь код по умолчанию
3. Вставьте следующий код:

```javascript
function doPost(e) {
  try {
    // Получаем активную таблицу
    const sheet = SpreadsheetApp.getActiveSpreadsheet().getActiveSheet();
    
    // Получаем данные из формы
    const name = e.parameter.name || '';
    const phone = e.parameter.phone || '';
    const message = e.parameter.message || '';
    
    // Получаем текущую дату и время
    const timestamp = new Date();
    const dateTime = Utilities.formatDate(timestamp, Session.getScriptTimeZone(), 'dd.MM.yyyy HH:mm:ss');
    
    // Добавляем строку в таблицу
    sheet.appendRow([dateTime, name, phone, message]);
    
    // Возвращаем успешный ответ
    return ContentService.createTextOutput(JSON.stringify({
      'status': 'success',
      'message': 'Данные успешно сохранены'
    })).setMimeType(ContentService.MimeType.JSON);
    
  } catch (error) {
    // Возвращаем ошибку
    return ContentService.createTextOutput(JSON.stringify({
      'status': 'error',
      'message': error.toString()
    })).setMimeType(ContentService.MimeType.JSON);
  }
}

// Функция для тестирования (опционально)
function doGet(e) {
  return ContentService.createTextOutput('Google Apps Script работает!');
}
```

4. Нажмите **Сохранить** (Ctrl+S или Cmd+S)
5. Дайте имя проекту, например: "Form Handler"

### Шаг 3: Разверните как веб-приложение

1. Нажмите **Развернуть** → **Новое развертывание**
2. Нажмите на иконку шестеренки ⚙️ → выберите **Веб-приложение**
3. Заполните форму:
   - **Описание**: "Form Handler для сайта"
   - **Выполнять от имени**: "Меня"
   - **У кого есть доступ**: "Все"
4. Нажмите **Развернуть**
5. **ВАЖНО**: При первом развертывании нажмите **Разрешить доступ** и авторизуйтесь
6. Скопируйте **URL веб-приложения** (он будет выглядеть так: `https://script.google.com/macros/s/.../exec`)

### Шаг 4: Обновите URL в коде

1. Откройте файл `src/components/ContactSection.tsx`
2. Замените `GOOGLE_SCRIPT_URL` на ваш новый URL:

```typescript
const GOOGLE_SCRIPT_URL = "ВАШ_URL_ИЗ_GOOGLE_APPS_SCRIPT";
```

3. Сохраните и задеплойте изменения

## ✅ Готово!

Теперь все заявки с сайта будут автоматически сохраняться в вашу Google Таблицу.

## 🔧 Дополнительные настройки

### Добавление уведомлений на email

Добавьте в конец функции `doPost` перед `return`:

```javascript
// Отправка email уведомления
const email = 'ваш-email@gmail.com'; // Замените на ваш email
const subject = 'Новая заявка с сайта CleanDvor';
const body = `
Новая заявка получена!

Имя: ${name}
Телефон: ${phone}
Сообщение: ${message}
Дата: ${dateTime}
`;

MailApp.sendEmail(email, subject, body);
```

### Добавление дополнительных полей

Если хотите добавить больше полей в форму:

1. Обновите форму в `ContactSection.tsx`
2. Добавьте поле в `formData`
3. Добавьте `formDataToSend.append("новое_поле", значение)`
4. Обновите Google Apps Script, добавив обработку нового поля
5. Добавьте новый столбец в Google Таблицу

### Форматирование данных

Вы можете улучшить форматирование в Google Apps Script:

```javascript
// Форматирование телефона
const formattedPhone = phone.replace(/\D/g, ''); // Удаляем все нецифровые символы

// Форматирование имени (первая буква заглавная)
const formattedName = name.charAt(0).toUpperCase() + name.slice(1).toLowerCase();
```

## 🐛 Решение проблем

### Форма не отправляется

1. Проверьте, что URL правильный в `ContactSection.tsx`
2. Убедитесь, что веб-приложение развернуто с доступом "Все"
3. Проверьте консоль браузера (F12) на наличие ошибок

### Данные не сохраняются в таблицу

1. Убедитесь, что вы авторизовались в Google Apps Script
2. Проверьте, что таблица открыта и активна
3. Проверьте логи в Google Apps Script: **Выполнение** → посмотрите последние выполнения

### Ошибка CORS

Если видите ошибку CORS, убедитесь, что в коде используется `mode: "no-cors"` (уже настроено)

## 📝 Структура таблицы

Рекомендуемая структура Google Таблицы:

| Дата и время | Имя | Телефон | Сообщение |
|-------------|-----|---------|-----------|
| 01.01.2024 10:30:00 | Иван Иванов | +79991234567 | Нужна уборка снега |

Можно добавить дополнительные столбцы:
- **Статус** (Новая, В работе, Выполнена)
- **Менеджер** (кто обрабатывает заявку)
- **Комментарий**

## 🔒 Безопасность

⚠️ **Важно**: URL веб-приложения будет публичным. Для дополнительной безопасности:

1. Добавьте проверку источника запроса в Google Apps Script
2. Используйте защиту от спама (например, reCAPTCHA)
3. Регулярно проверяйте таблицу на наличие подозрительных записей

Пример защиты от спама в Google Apps Script:

```javascript
// В начале функции doPost
const allowedDomains = ['cleandvor.ru', 'famellow.github.io']; // Ваши домены
const referer = e.parameter.referer || '';
const isValidDomain = allowedDomains.some(domain => referer.includes(domain));

if (!isValidDomain) {
  return ContentService.createTextOutput(JSON.stringify({
    'status': 'error',
    'message': 'Unauthorized'
  })).setMimeType(ContentService.MimeType.JSON);
}
```
