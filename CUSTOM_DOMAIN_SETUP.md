# Настройка кастомного домена для CleanDvor

## 🎯 Дешевые варианты доменов

### Вариант 1: Дешевые домены (.ru, .xyz, .site)
- **Reg.ru** — от 99₽/год для .ru доменов
- **Namecheap** — от $0.99/год для .xyz доменов (часто промо)
- **Cloudflare Registrar** — домены по себестоимости (самый дешевый вариант!)
- **NameSilo** — дешевые домены с бесплатным WHOIS privacy

### Рекомендация: Cloudflare Registrar
- Самые низкие цены (домены по себестоимости)
- Бесплатный WHOIS privacy
- Бесплатный DNS
- Легкая настройка

## 📋 Пошаговая инструкция

### Шаг 1: Купите домен
1. Зайдите на [Cloudflare Registrar](https://www.cloudflare.com/products/registrar/) или другой регистратор
2. Найдите и купите домен (например, `cleandvor.ru` или `cleandvor.xyz`)
3. После покупки домен будет автоматически добавлен в ваш Cloudflare аккаунт

### Шаг 2: Настройте DNS записи в Cloudflare

1. Зайдите в Cloudflare Dashboard → выберите ваш домен
2. Перейдите в раздел **DNS** → **Records**
3. Добавьте следующие записи:

#### Для основного домена (apex domain):
```
Тип: A
Имя: @
IPv4 адрес: 185.199.108.153
Proxy: выключен (серый облачко)
TTL: Auto
```

Повторите для всех IP адресов GitHub Pages:
- `185.199.108.153`
- `185.199.109.153`
- `185.199.110.153`
- `185.199.111.153`

#### Для поддомена www:
```
Тип: CNAME
Имя: www
Целевой домен: famellow.github.io
Proxy: выключен (серое облачко)
TTL: Auto
```

### Шаг 3: Настройте домен в GitHub Pages

1. Зайдите в ваш репозиторий на GitHub
2. Перейдите в **Settings** → **Pages**
3. В разделе **Custom domain** введите ваш домен (например, `cleandvor.ru`)
4. Поставьте галочку **Enforce HTTPS** (после настройки DNS)
5. Нажмите **Save**

### Шаг 4: Обновите файл CNAME

После добавления домена в GitHub, обновите файл `public/CNAME`:

```bash
echo "cleandvor.ru" > public/CNAME
```

Или если используете www:
```bash
echo "www.cleandvor.ru" > public/CNAME
```

**Важно:** После этого закоммитьте и запушьте изменения:
```bash
git add public/CNAME
git commit -m "Add custom domain"
git push origin main
```

Конфигурация проекта автоматически определит наличие кастомного домена и использует правильный base path.

### Шаг 6: Подождите распространения DNS

DNS изменения могут занять от нескольких минут до 48 часов. Обычно это занимает 1-2 часа.

Проверить можно командой:
```bash
dig cleandvor.ru +short
```

## 🔧 Альтернативный вариант: Использовать только www

Если хотите использовать только `www.cleandvor.ru` (без apex domain):

1. В DNS создайте только CNAME запись для www
2. В GitHub Pages укажите `www.cleandvor.ru`
3. Настройте редирект с apex на www в Cloudflare (Page Rules)

## 💡 Важные моменты

1. **HTTPS**: GitHub Pages автоматически предоставляет бесплатный SSL сертификат через Let's Encrypt
2. **Проверка**: После настройки проверьте сайт через [SSL Labs](https://www.ssllabs.com/ssltest/)
3. **Кэш**: После изменений DNS может потребоваться очистка DNS кэша браузера

## 🚀 Автоматизация через GitHub Actions

После настройки домена, GitHub Actions будет автоматически деплоить сайт на ваш кастомный домен при каждом push в main.

## 📞 Проблемы?

Если домен не работает:
1. Проверьте DNS записи через `dig` или онлайн инструменты
2. Убедитесь, что в GitHub Pages домен указан правильно
3. Проверьте, что прошло достаточно времени для распространения DNS (до 48 часов)
4. Убедитесь, что галочка "Enforce HTTPS" включена только после того, как SSL сертификат будет выдан
